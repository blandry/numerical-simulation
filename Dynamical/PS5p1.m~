N = 500;
dx = 1/N;
x0 = zeros(N+1,1);

dt = 0.001;
tf = 5;

fhand=@(x,u) fbar(x,u,dx);
dfhand=@(x,u) dfbar(x,u,dx);

x = traprule(fhand,dfhand,x0,tf,dt);

plot(1:500,x(:,end),1:500,x(:,end-100),1:500,x(:,end-200),1:500,x(:,end-300));